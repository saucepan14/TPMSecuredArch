# TPMSecuredArch
This repo contains various files that allow Arch Linux to be securley encrypted and unlocked without requiring a password. It uses a luks based encrypted root file system and adds a systemd unit to the initramfs to get the key from the tpm.
